class Draft {
  final String id;
  final String description;
  final String receiver;
  final double value;
  final DateTime date;

  const Draft(
      {this.id, this.description, this.value, this.date, this.receiver});
}
